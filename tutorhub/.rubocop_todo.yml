# Auto-generated by RuboCop — keep this file checked in so the
# existing style baseline doesn't fight a fresh clone.

# Empty placeholder. Run `bundle exec rubocop --auto-gen-config` once
# you've accepted existing offenses.
